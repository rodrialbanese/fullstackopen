{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Iniciar Chrome para localhost",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}"
        },
        {
            "type": "node-terminal",
            "name": "Ejecutar el script: start",
            "request": "launch",
            "command": "npm run start",
            "cwd": "${workspaceFolder}\\part4\\blog"
        }
    ]
}